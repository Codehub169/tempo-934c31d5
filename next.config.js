/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Future configurations can be added here, for example, experimental features or image domains.
  // For serving SVGs from the public folder, no specific Next.js config is needed.
  // If SVGs were to be imported as React components, a webpack loader would be configured here.
};

module.exports = nextConfig;